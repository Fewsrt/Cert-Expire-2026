# Install on the Ansible controller (the machine that runs ansible-playbook), not on guests.
#
# RHEL / Fedora — if `No module named pip`:
#   dnf install -y python3-pip
#   cd ansible && python3 -m pip install -r controller-pip-requirements.txt
pywinrm>=0.4.3
requests>=2.28.0
