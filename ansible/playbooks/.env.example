# Copy to .env (gitignored) and set your shared password:
#   cp .env.example .env
#
# Shell:
#   set -a && source .env && set +a
#   ansible-playbook playbooks/secureboot_ca_assessment.yml
#
ANSIBLE_PASSWORD=
