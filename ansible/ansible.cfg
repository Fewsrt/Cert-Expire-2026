[defaults]
inventory = inventory/vcenter_csv_inventory.py
# `yaml` callback is not always present on minimal ansible-core installs (use default).
stdout_callback = default
retry_files_enabled = False
host_key_checking = False
interpreter_python = auto_silent

[inventory]
enable_plugins = script, yaml, ini
